export{}

// const num : number = Math.round(Math.random() * 5)
const num: number = 2

switch (num){
  case 1:
    console.log(`ケース${num}です。`);
    break;
  case 2:
    console.log(`ケース${num}です。`);
    break;
  case 3:
    console.log(`ケース${num}です。`);
    break;
  case 4:
    console.log(`ケース${num}です。`);
    break;
  case 5:
    console.log(`ケース${num}です。`);
    break;
  default:
    console.log(`該当なしです。`)
}
